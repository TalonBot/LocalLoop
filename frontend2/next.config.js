/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "pdhffuuykopnzhnlgxie.supabase.co",
      // add other domains if needed
    ],
  },
};

module.exports = nextConfig;
